# 项目代码生成规则

## 1. 修改与验证要求

本文件是本仓库的代码生成与修改规范。除系统指令和用户当前明确要求外，后续新增或修改代码必须遵守以下规则。规则适用于整个仓库，子目录中的更具体 `AGENTS.md` 可以补充本文件，但不能降低本文件的强制要求。
每次改动
请像代码审查一样汇报：
1．修改了哪些文件，每个修改解决什么问题；
2．原始需求分别对应哪些代码变化；
3．实际执行过哪些测试或验证命令，关键输出是什么；
4．哪些旧功能可能受到影响，如何确认没有回归；
5．哪些场景还没有验证，仍然存在什么风险。

如果存在不确定，请明确指出。
不要用“完成”“没问题”“可以上线”
代替验证结果。

## 2. 禁止事项

- 禁止提交无关的格式化、依赖升级、配置重写或删除用户已有改动。
- 禁止使用破坏性 git 命令覆盖用户工作区内容。
